kind = "web"
previewPath = "/"
title = "Нейро 32"
version = "1.0.0"
id = "artifacts/neuro32"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/neuro32 run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/neuro32", "run", "build" ]
publicDir = "artifacts/neuro32/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
